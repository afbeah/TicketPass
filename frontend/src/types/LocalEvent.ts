export interface LocalEvent {
    eventId: string
    name: string
    description: string
    location: string
    startDateTime: string
    endDateTime: string
    ticketId: string
    ticketPrice: number
}